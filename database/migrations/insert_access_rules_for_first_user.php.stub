<?php

use App\Models\User;
use Illuminate\Database\Migrations\Migration;
use Wnikk\LaravelAccessRules\Facades\Access;

/**
 * An example to start from: two rules, the role "RootAdmin" that holds them, and the first user
 * of the application as a member of that role. Edit it to the rules of your project before "migrate".
 *
 * It expects "Role" and App\Models\User in "owner_types" of config/access.php,
 * and the trait Wnikk\LaravelAccessRules\Traits\HasPermissions on the User model.
 */
return new class extends Migration
{
    /**
     * Rules are the things that can be permitted. A rule is created once, in a migration.
     */
    protected function addRules(): void
    {
        Access::newRule(
            'Test.rule',
            'Test rule for first user',
            'This is a test rule for first user with validation',
            options: 'nullable|string|in:test,example,demo' // A permission may name one of these options
        );
    }

    /**
     * A role is an owner of permissions like a user is, only without a model behind it.
     */
    protected function addRoleAdmin(): void
    {
        Access::for('Role', 'RootAdmin')->create('RootAdmin role');

        // Other owners are addressed the same way, the type comes from "owner_types":
        //Access::for('Group', 123)->create('Editors');
        //Access::for('Group', '23f45e89-8b5a-5c55-9df7-240d78a3ce15')->create('Partners');
        //Access::for($user);   // a model, its owner is created on the first permission
    }

    protected function addPermToAdmin(): void
    {
        $root = Access::for('Role', 'RootAdmin');

        // Without an option, because the rule lets it be empty, and then one by one with options
        $root->allow('Test.rule');
        $root->allow('Test.rule', 'test');
        $root->allow('Test.rule', 'demo');
        $root->allow('Test.rule', 'example');
    }

    /**
     * Permissions with conditions (ABAC): they work only for records the condition is true for.
     *
     * A condition about a record needs to know its model: list it in config/access.php
     *     'resources' => ['order' => App\Models\Order::class, 'item' => App\Models\OrderItem::class],
     * and add the trait Wnikk\LaravelAccessRules\Traits\HasAccessScope to the model to filter lists.
     * The examples below are commented out, because models of your application are not known here.
     */
    protected function addConditionalPermissions(): void
    {
        $root = Access::for('Role', 'RootAdmin');

        /**
         * A condition that is only about the user or the environment needs no record and no model
         */
        Access::newRule('Test.worktime', 'Test rule that works on working days only');
        $root->allow('Test.worktime', when: 'env.weekday in [1, 2, 3, 4, 5]');

        /**
         * The rule is about orders: "order." in conditions is the record that is checked
         */
        //Access::newRule('orders.view', 'View orders', resource: 'order');
        //Access::newRule('orders.update', 'Update orders', resource: 'order');

        // columns of the record, number of related records
        //$root->allow('orders.view', when: 'order.cost > 100 && order.items.count < 3');

        // the record against the user, a related record, state of the record
        //$root->allow('orders.update', when: "order.department_id == user.department_id && order.status in ['draft', 'review']");
        //$root->allow('orders.update', when: 'order.client.manager_id == user.id');

        // an aggregate with a filter: clients with turnover of the last month above 1000
        //Access::newRule('clients.view', 'View clients', resource: 'client');
        //$root->allow('clients.view', when: 'sum(client.payments.amount, paid_at >= monthStart(-1) && paid_at < monthStart(0)) > 1000');

        // a prohibition for one owner on top of what it inherits from a role
        //$user->addProhibition('clients.view', when: "client.city == 'Y'");

        /**
         * How it is used then:
         *     $user->can('orders.view', $order);                     // this very order
         *     $user->can('orders.view', Order::class);               // orders in general: menu, list page
         *     Order::query()->allowedTo('orders.view')->paginate();  // only orders the current user may see, one query
         */
    }

    /**
     * @return (User&\Wnikk\LaravelAccessRules\Traits\HasPermissions)|null
     */
    protected function firstUser(): ?User
    {
        return User::orderBy('id')->first();
    }

    /**
     * The first user gets everything the role has, now and later.
     */
    protected function addInheritFromAdmin(): void
    {
        $this->firstUser()?->inheritPermissionFrom('Role', 'RootAdmin');

        // Or permissions of the user itself, next to the role or instead of it
        //$user->addPermission('Test.rule');
        //$user->addPermission('Test.rule', 'test');
    }

    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        // Cached permissions are reset once, after all changes, and not after every line
        Access::batch(function () {
            $this->addRules();
            $this->addRoleAdmin();
            $this->addPermToAdmin();
            $this->addConditionalPermissions();
            $this->addInheritFromAdmin();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Access::batch(function () {
            $this->firstUser()?->remInheritFrom('Role', 'RootAdmin');

            // "true": the role holds these rules, and a rule somebody holds is not deleted without it
            Access::delRule('Test.rule', true);
            Access::delRule('Test.worktime', true);

            Access::for('Role', 'RootAdmin')->delete();
        });
    }
};
