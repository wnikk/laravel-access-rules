language: php

dist: trusty

php:
  - 8.0
  - 8.1
  - 8.2
  - hhvm

matrix:
  fast_finish: true
  allow_failures:
    - php: 8.2

before_script:
  - travis_retry composer self-update
  - travis_retry composer install --no-interaction --prefer-source
